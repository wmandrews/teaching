// write text in the console
var myText = 'Hello console';
console.log(myText);

// write text in the console with a function
function debug(txt) {
    console.log('Debug: ' + txt);
}

//debug('Hello again');

// write text into a page element
// var log = document.getElementById('log');
// log.innerHTML = 'Hello world';

// arrays
/*
var names = ['Jane','Sally','Joe','Mark'];
var ages = [8,12,17,13];
var colors = ['blue','green','purple','red'];

// loop through the arrays and write out their values in the console
// array[0] = the first value in an array, which is why we start i out at zero
// array.length = the amount of items in an array, which is why loop until we hit that number
for (var i=0; i<names.length; i++) {
    console.log('Name: ' + names[i]);
    console.log('Age: ' + ages[i]);
    console.log('Color: ' + colors[i]);
}
*/

/*
var myContainer = document.getElementById('container');
myContainer.innerHTML ='<table>'
myContainer.innerHTML+='<thead>'
myContainer.innerHTML+='<tr>'
myContainer.innerHTML+='<th>Header</th>'
myContainer.innerHTML+='</tr>'
myContainer.innerHTML+='</thead>' 
myContainer.innerHTML+='<tbody>'
myContainer.innerHTML+='<tr>'
myContainer.innerHTML+='<td>Table cell</td>'
myContainer.innerHTML+='</tr>'
myContainer.innerHTML+='</tbody>' 
myContainer.innerHTML+='</table>';  
*/